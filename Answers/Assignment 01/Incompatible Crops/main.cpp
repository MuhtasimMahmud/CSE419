#include <bits/stdc++.h>

using namespace std;

int main()
{

    ios::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);



    int x,y;
    cin>>x>>y;

    int arr[x][y];


    int row_length = sizeof(arr)/sizeof(arr[0]);
    int column_length = sizeof(arr)/sizeof(arr[0][0]);



    char c;
    for(int i=0; i<x; i++)
    {
        string s = "";
        for()
    }

    for(int i=0; i<row_length; i++)
    {
        for(int j = 0; j<column_length; i++)
        {

            arr[i][j] = s[j];
        }
    }


    cout << "Hello world!" << endl;
    return 0;
}
